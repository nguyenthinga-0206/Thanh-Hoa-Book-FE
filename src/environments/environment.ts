// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.
// import {initializeApp} from "firebase/app";

export const environment = {
  production: true,
  firebaseConfig: {
    apiKey: "AIzaSyD7ikTz5y2bKw4mKYYn5ZEASkIz7Kg-gdg",
    authDomain: "honnya-img.firebaseapp.com",
    databaseURL: "https://honnya-img-default-rtdb.asia-southeast1.firebasedatabase.app",
    projectId: "honnya-img",
    storageBucket: "honnya-img.appspot.com",
    messagingSenderId: "266973106524",
    appId: "1:266973106524:web:ecb11dac3c5b2d62e0f199",
    measurementId: "G-NLD38WWXTZ"
  }
};

// Initialize Firebase
// const app = initializeApp(environment.firebaseConfig);

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
