(function ($) {
  "use strict";

  var tpj = jQuery;
  var revapi24;
  // Preloader
  jQuery(document).ready(function () {
    $("#datepicker").datepicker({
      format: "yyyy",
      viewMode: "years",
      minViewMode: "years"
    });
  })
})(jQuery);
